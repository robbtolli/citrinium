/// Pure-Dart vault I/O, Markdown parser/serializer, and index layer for
/// Citrinium. No Flutter import is possible from this package (see
/// `AGENTS.md` / `design.md` §6.1).
library;

export 'parser.dart';
export 'vault.dart';
